/** Props every Signals & Spectra section receives from the module shell. */
export interface SectionProps {
  /** Shared animation clock in seconds (currently fixed at 0 — no auto-play). */
  clock: number;
}
