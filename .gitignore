# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test coverage
coverage/

# Stray local test output (captured `npm test` redirects)
test-output.txt

# Course materials — instructor's private PDFs (textbook + lecture slides).
# Excluded by default because the repo may be published for GitHub Pages.
# Remove this line if you intend to version the reference PDFs.
refs/

# Local AI / project config — not versioned.
# docs/book-reference.md IS versioned; only the local design docs under
# docs/superpowers/ (specs, plans, archives) stay out of the repo.
.claude/
CLAUDE.md
docs/superpowers/
refs/
# Visual brainstorming companion (local-only)
.superpowers/

# Local todo / roadmap (not for the repo)
TODO.md
